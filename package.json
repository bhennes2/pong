{
  "name": "RocketPong",
  "version": "1.0.0",
  "description": "Ping Pong LaunchPad Lab style",
  "scripts": {
    "start": "meteor --settings settings/development.json",
    "deploy:staging": "meteor deploy staging.rocketpaddle.com --settings settings/staging.json",
    "deploy:production": "meteor deploy www.rocketpaddle.com --settings settings/production.json"
  }
}
